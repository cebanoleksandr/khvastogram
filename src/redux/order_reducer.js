


const orderReduser = (state, action) => {
    switch (action.type) {
        // case BUY_GOODS:
        //     state.orders.push(action.newOrder);
        //     console.log(state.orders);
        //     return state;

        default:
            return state;
    }
}

// action creators


export default orderReduser;
